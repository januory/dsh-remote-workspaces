node_modules/

# Transient artifacts
*.log
*.tmp

# npm lockfile (repo is pnpm-managed; the release workflow never commits it)
package-lock.json

# Local design docs (never committed)
docs/

# Built client bundle (generated by `pnpm run build:client`; not committed)
lib/
